name: 'dbt_airbnb'
version: '1.0.0'
config-version: 2

profile: 'dbt_airbnb'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  dbt_airbnb:
    staging:
      +materialized: view
    marts:
      +materialized: table